# Optional: override hardcoded ports (see backend/config.py for backend port)
# Frontend dev server port when running npm start
PORT=3001
# Backend API URL when using npm start (must match backend port)
REACT_APP_API_URL=http://localhost:8000
